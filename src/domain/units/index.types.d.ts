export type Minutes = number & { readonly brand: unique symbol }
export type Seconds = number & { readonly brand: unique symbol }
export type UnixTimestamp = number & { readonly brand: unique symbol } // in seconds
export type Satoshis = number & { readonly brand: unique symbol }
export type MilliSatoshis = number & { readonly brand: unique symbol }